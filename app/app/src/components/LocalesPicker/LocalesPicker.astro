---
export interface Props
{
	lang?: string | undefined
}

const {
	lang = Astro.currentLocale,
} = Astro.props

// TODO: Get the path for the current page to link to the same page in other locales
// do that in static rendering mode
---

<ul class="lang-list">
	<li>
		<a href="/" class:list={[ lang === 'fr' && 'active' ]}>
			<span class="icon icon-[twemoji--flag-france] icon-align"></span>
		</a>
	</li>
	<li>
		<a href="/en" class:list={[ lang === 'en' && 'active' ]}>
			<span class="icon icon-[twemoji--flag-united-kingdom] icon-align"></span>
		</a>
	</li>
</ul>

<style lang="scss">
@reference "tailwindcss/theme";

.lang-list {
	@apply flex items-center mb-4 gap-2;

	.icon {
		@apply text-2xl;
	}

	a {
		@apply opacity-50;

		&:hover {
			@apply opacity-75;
		}

		&:active, &.active {
			@apply opacity-100;
		}
	}
}
</style>
